# EEEE C# backend

Implementation slot reserved for the C# backend. It must follow [`../CONTRACT.md`](../CONTRACT.md). The adapter should provide ASP.NET Core endpoints, SQLite persistence, and the same API contract.
