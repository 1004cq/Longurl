# EEEE Java backend

Implementation slot reserved for the Java backend. It must follow [`../CONTRACT.md`](../CONTRACT.md). The adapter should provide a standard HTTP server, SQLite/JDBC persistence, and the same API contract.
