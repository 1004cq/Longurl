# EEEE Rust backend

Implementation slot reserved for the Rust backend. It must follow [`../CONTRACT.md`](../CONTRACT.md). Build and database integration should be verified in a Rust toolchain before release.
